{
  "name": "@exilecompass/wealth-addon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/panel.ts --bundle --format=esm --platform=browser --outfile=dist/panel.js",
    "test": "bun test test/valuation.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
